// window object = browser api
// within the window object we have the document object
// to console properties and methods of a window object
// we have to use: console.dir(object)

console.dir(document);
